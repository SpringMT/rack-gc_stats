require "spec_helper"

RSpec.describe Rack::GcStat do
  it "has a version number" do
    expect(Rack::GcStat::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
